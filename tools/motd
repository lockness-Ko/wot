WOT (without a trace)

Welcome!

Here are some useful commands:
Web browser
 - `lynx`
 - `browsh` (less secure because it can run js but it looks prettier and can run js sites)

Networking
 - `nc`
 - `ncat`
 - `wget`
 - `curl`

Text editing
 - `nano`
 - `vi`
